package cooperpay.controller;

/**
 * Controller desativado. 
 * A atualização de status agora é feita diretamente no PagamentoServe,
 * pois o sistema não utiliza Webhooks externos.
 */
public class AsaasWebhookController {
    // Removido para simplificação do ambiente local
}